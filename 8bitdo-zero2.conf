[ids]
2dc8:9018	

[main]
# Start -> Enter (common in games to confirm/start)
o = enter

# Select -> Backspace (often used as "back" or "cancel")
n = backspace

# D-Pad mappings to arrow keys
c = up
d = down
e = left
f = right

# Main face buttons mapped to intuitive keys
g = a
j = b
h = x
i = y

# Bumpers mapped to Shift and Control for flexibility
# NOTE: these aren't working for some reason!
#k = leftshift   # Left bumper as Shift (modifier key)
#m = leftctrl    # Right bumper as Control (can be used for advanced mappings)

